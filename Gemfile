source "https://rubygems.org"
gemspec

# Pin jekyll-sass-converter to the 2.x line so it uses the pure-Ruby `sassc`
# instead of `sass-embedded`, whose native binary download fails in CI.
gem "jekyll-sass-converter", "~> 2.0"
